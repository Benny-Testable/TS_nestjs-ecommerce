# Security overview

Application security controls are implemented in `src/common/security` and `src/api/auth`. Infrastructure controls are in `infra/terraform`. Policies are in `docs/compliance/soc2` and `docs/compliance/ferpa`. Intake: `/SECURITY.md`.
