# Go inventory service

Stock reservation for `inventory.entity.ts` (`productVariationId`, `countryCode`, `quantity`).

```bash
go test -C services/inventory ./...
go test -C services/inventory -cover ./...
```
